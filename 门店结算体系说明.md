# 门店结算体系说明

> 适用范围：将军世家小程序商城 —— 自提订单的**跨店货款结算**
> 代码位置：`server/app/common/logic/StoreSettlementLogic.php`、`server/app/adminapi/logic/store_settlement/`
> 最后更新：2026-09-22

---

## 一、业务背景

平台采用「线上下单 + 门店自提」模式，存在如下经营场景：

- 顾客在 A 门店所属的小程序/商品页下单，订单归属 A 门店（**A 是可得分账的一方**）；
- 顾客实际到 B 门店提货，由 B 门店的员工核销（**B 是实际交货、提供履约服务的一方**）。

此时 A 收了钱、B 出了货，A 必须把货款结算给 B。

系统据此建立**门店结算中心**：以「核销动作」为触发点，自动沉淀跨店结算明细，再按日/周/月生成汇总账单，线下付款后在后台登记留痕。

---

## 二、术语与角色

| 术语 | 字段 | 含义 |
| --- | --- | --- |
| 归属门店（付款方） | `order.belong_store_id` → `pay_store_id` | 下单时订单归属的门店，收了顾客的钱，**要向对方付钱** |
| 核销门店（收款方） | `verification.handle_store_id` → `receive_store_id` | 实际交货并核销的门店，提供服务，**应收钱** |
| 平台账号 | `admin.store_id = 0` | 不归属任何门店的后台账号，**不参与**门店间结算 |
| 门店账号 | `admin.store_id = 门店ID` | 归属某门店的后台账号，其核销动作代表该门店履约 |

> 核销门店的取值来源：**核销操作员所属门店（`admin.store_id`）**；
> 若操作员是平台账号（`store_id = 0`），则回退取订单自身的 `selffetch_shop_id`。

---

## 三、参与范围与触发条件

结算明细**只**在同时满足以下条件时生成：

1. 订单为**自提订单**：`order.delivery_type == DeliveryEnum::SELF_DELIVERY`
2. 付款方与收款方均为有效门店：`pay_store_id > 0` 且 `receive_store_id > 0`
3. **确实发生了跨店**：`pay_store_id != receive_store_id`

不满足任一条 → 不生成明细（同店核销、快递订单均不参与）。
同一订单**幂等**：`ls_store_settlement_order` 中已存在该 `order_id` 则直接跳过。

---

## 四、数据模型

### 4.1 门店表（比例配置）

`ls_selffetch_shop`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `settlement_ratio` | decimal(5,2) | 该门店作为**付款方**时的结算比例(%)；`0` = 跟随全局配置 |

### 4.2 结算明细表

`ls_store_settlement_order`（一条 = 一笔跨店核销）

| 字段 | 说明 |
| --- | --- |
| `order_id` / `order_sn` | 关联订单 |
| `pay_store_id` | 付款门店（归属门店） |
| `receive_store_id` | 收款门店（核销门店） |
| `amount` | 本单结算金额（= 各行 `amount` 之和） |
| `base_price` | 结算基数（成交价总额），供审计 |
| `ratio` | 本次计算实际使用的比例(%)，**固化当时口径**，日后改比例仍可还原 |
| `cost_source` | 取价来源：`1`按比例 / `2`按商品协商价 / `3`取价失败(待复核) |
| `detail` | JSON 商品行明细 |
| `status` | `0`未入账 / `1`已入账 |
| `settlement_id` | 所属汇总账单 ID，`0` = 尚未入账 |

`detail` JSON 结构（每行）：

```json
{
  "goods_name": "商品名",
  "goods_num": 2,
  "supply_price": 12.50,   // ⚠️ 语义为「单件结算单价」，字段名历史遗留不可改
  "base_price": 25.00,     // 单件成交价（仅供排查，前端不展示）
  "ratio": 50,             // 本次所用比例（仅供排查，前端不展示）
  "amount": 25.00          // 行结算额 = 单件结算单价 × 数量
}
```

### 4.3 汇总账单表

`ls_store_settlement`（一条 = 某账期内某门店对的账单）

| 字段 | 说明 |
| --- | --- |
| `sn` | 账单编号 |
| `period_type` | `1`日 / `2`周 / `3`月 |
| `start_date` / `end_date` | 账期区间 |
| `pay_store_id` / `receive_store_id` | 付款门店 / 收款门店 |
| `amount` | 账单总额 |
| `order_count` | 明细条数 |
| `status` | `0`待确认 / `1`已确认 / `2`已付款 |
| `create_admin_id` | 生成人 |
| `confirm_time` / `confirm_admin_id` | 确认时间 / 确认人（留痕） |
| `pay_time` / `pay_admin_id` | 付款时间 / 标记付款人（留痕） |

### 4.4 关联表

| 表 | 字段 | 说明 |
| --- | --- | --- |
| `ls_admin` | `store_id` | 管理员所属门店，`0` = 平台账号 |
| `ls_verification` | `belong_store_id` | 核销时订单的归属门店 |
| `ls_verification` | `handle_store_id` | 核销操作员所属门店（= 收款方） |
| `ls_config` | `type='store_settlement'` | `mode` 结算模式、`ratio` 全局比例 |

---

## 五、业务流程

```
顾客下单(归属门店A)
        │
        ▼
顾客到门店B提货，B或平台员工核销
        │  VerificationLogic::verification()
        ├──► 写 ls_verification（含 belong_store_id / handle_store_id）
        ├──► StoreSettlementLogic::createForOrder()  ← 跨店则生成明细
        └──► 订单状态置为「已完成 / 已核销」
                        │
                        ▼
        后台「生成账单」SettlementLogic::generate()
        按 (门店对 + 账期) 分组 → 追加待确认账单 / 新建账单
                        ▼
              确认账单 → status 1（记录确认人）
                        ▼
            标记付款 → status 2（记录付款人，含一致性校验）
```

关键点：**核销与结算是解耦的**。结算生成失败只写错误日志，**不阻断核销主流程**（核销本身已成功，不能因结算异常让顾客提不了货）。

---

## 六、结算口径与规则

### 6.1 结算模式（`ls_config` → `store_settlement.mode`）

**模式 1（默认，推荐）：按「成交价 × 比例」**

```
单件成交价 = (行实付商品金额 total_pay_price − 该行运费 express_price) ÷ 数量
单件结算单价 = 单件成交价 × 比例 ÷ 100        （四舍五入 2 位）
行结算额 = 单件结算单价 × 数量
```

**模式 0（兼容旧口径）：按商品协商价**

取价顺序：下单快照 `goods_snap.supply_price` → 规格表 `goods_item.supply_price` → `goods_item.cost_price`，均取不到返回 0。

> 切换为模式 1 的原因：实测库中 `supply_price` 全库仅 0/1331 有值、`cost_price` 仅 1/1331 有值，且后台无维护入口 —— 沿用旧口径会导致结算金额恒为 0、无法出账。

### 6.2 比例优先级

```
归属门店(付款方)的 settlement_ratio > 0 ？ 用它
                否则                → 用全局 ratio
```

> 比例取「**付款方**」的配置（谁付钱按谁的标准），收款方自身的比例不影响本次结算。

### 6.3 异常兜底（不静默出错）

- 任一行结算单价 `≤ 0` → 整单 `cost_source = 3`，金额为 0；
- 后台结算明细以**红色「待复核」标签**展示，需人工介入；
- `markPaid` 会拦截 0 元账单，防止异常账单被误付款。

### 6.4 账期归属规则（已修复漏账）

明细归入哪个账期，由**明细自身的 `create_time`** 决定，而非先按日期区间筛明细。

> 修复前的问题：旧实现「按给定日期区间捞明细 + 同账期同门店已存在账单就跳过」。
> 只要在账期中途生成过一次账单，该账期之后新增的明细会被两边同时排除（提交时因账单已存在被跳过、下个账期又因 `create_time` 不在新区间捞不到），形成**永久漏账且无告警**。

修复后的生成算法：

1. 捞出全部**未入账**明细（`status = 0 AND settlement_id = 0`），不按时间过滤；
2. 每条明细按自身 `create_time` 归入所属账期（日/周/月）；
3. 按 `(pay_store_id, receive_store_id, start_date, end_date)` 分组；
4. 每组：存在同账期同门店对的**待确认**账单 → **追加**；否则 → **新建**。

已确认/已付款的账单**不会被追加**（避免改动既定账务），此时另建一张**补充账单**，账单列表中并存，由人工核对处理。

### 6.5 付款前一致性校验（防漏付）

`markPaid` 执行前强制校验：

1. 明细条数必须等于 `bill.order_count`，否则报「账单明细不完整」；
2. 明细金额合计必须等于 `bill.amount`（`bccomp` 精度 2），否则报「金额与明细合计不一致」；
3. 账单金额必须 `> 0`，否则报「账单金额为 0，无法标记付款」。

---

## 七、后台操作手册

### 7.1 配置管理员所属门店

**路径**：后台 → 设置 → 权限管理 → 管理员 → 编辑

「所属门店」下拉可选：

- `平台账号（不归属门店）` → `store_id = 0`，不参与门店间结算；
- 具体门店 → 该账号核销时以此门店作为**实际交货门店**参与结算。

> 修改所属门店后，该账号**所有已登录 token 立即失效**，需重新登录（防止门店权限错配后仍沿用旧身份操作）。

### 7.2 结算中心

**路径**：后台 → 应用 → 门店结算

| 操作 | 说明 |
| --- | --- |
| 生成账单 | 选择周期类型（日/周/月）与日期 → 汇总所有未入账明细 |
| 详情 | 查看账单商品行明细、结算金额、金额来源标签 |
| 确认 | 待确认 → 已确认，记录确认人 |
| 标记付款 | 已确认 → 已付款，记录付款人（线下付款后登记） |

---

## 八、接口清单

后台接口（自动路由，前缀 `/store_settlement.settlement/`）：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `generate` | 生成汇总账单 |
| GET | `lists` | 账单列表（可按状态、日期区间筛选） |
| GET | `detail` | 账单详情（含明细） |
| POST | `confirm` | 确认账单 |
| POST | `markpaid` | 标记付款 |
| GET | `config` | 读取结算配置 |
| POST | `saveConfig` | 保存结算配置 |

操作人 ID（`admin_id`）由**服务端从登录态获取**，前端无需传递。

---

## 九、运维工具

`server/migrations/store_settlement_config.php`（**CLI，建议以 www 用户执行**）

```bash
# 查看当前配置（含各门店比例）
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php

# 表结构迁移（幂等，可重复执行）
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php migrate

# 结算模式 1=按比例 / 0=按商品协商价
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php mode 1

# 全局比例 70%
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php ratio 70

# 门店 2 的独立比例 65%（0 = 跟随全局）
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php shop-ratio 2 65

# 列出全部门店及比例
sudo -u www /www/server/php/80/bin/php migrations/store_settlement_config.php shops
```

**两条强制约束**：

1. **必须用 PHP 8.0 及以下版本执行**。ThinkPHP 6.0 的 `think\Paginator` 未声明 `ArrayAccess` 返回类型，PHP 8.1+ 会产生弃用告警，而框架错误处理器会把它升级为 `ErrorException`，导致 CLI 下 `App::initialize()` 直接失败。
2. **需 root 执行时注意缓存属主**。本项目 `config/cache.php` 的 default 硬编码为 `file`（`.env` 里的 `[CACHE] DRIVER=redis` 实际未生效），缓存目录 `runtime/cache/08/` 属主为 www。脚本已刻意设计为「**只删缓存、不写缓存**」（读配置直查库、写配置走 `ConfigService::set`），可安全以 root 执行。

---

## 十、迁移脚本清单

| 文件 | 内容 |
| --- | --- |
| `migrations/20260921_store_settlement_ratio.sql` | `ls_selffetch_shop.settlement_ratio`；`ls_store_settlement_order.base_price`、`ratio` |
| `migrations/20260921_store_settlement_operators.sql` | `ls_store_settlement.confirm_admin_id`、`pay_admin_id` |
| `migrations/store_settlement_config.php` | 上述结构迁移（幂等）+ 配置读写工具 |

---

## 十一、已知限制与注意事项

| 项 | 说明 |
| --- | --- |
| 结算配置无前端入口 | `config` / `saveConfig` 接口已具备，但后台**暂无对应页面**，`web/src/api/application/store_settlement.ts` 也未导出该接口。当前需通过 CLI 工具或直接调接口配置。 |
| 全局比例为 0 时无法出账 | `store_settlement.ratio = 0` 时，模式 1 下所有明细金额为 0 并标记「待复核」。**上线前必须先配置全局比例或门店独立比例。** |
| `cost_source = 1` 前端显示「协议价」 | 前端硬编码 `['协议价','成本价','待复核']`，语义与「按成交价×比例」不符，属历史遗留。改了会影响取值对齐，改动需谨慎。 |
| 生成账单弹窗文案过时 | 弹窗提示仍为「已生成过的周期会自动跳过」，实际逻辑已改为「追加到待确认账单 / 另建补充账单」，文案待同步。 |
| 账单不可撤销 | 已确认/已付款账单无作废入口，异常需线下处理。 |
| 平台账号核销 | `store_id = 0` 的账号核销时，收款方回退为订单的 `selffetch_shop_id`，可能产生非预期的跨店明细，建议由门店账号执行核销。 |
