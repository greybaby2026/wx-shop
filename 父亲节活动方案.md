# 将军世家 · 父亲节充值抵扣活动方案

> 版本：v1.0 | 日期：2026-05-19 | 状态：待审核

---

## 一、活动概述

| 项目 | 内容 |
|------|------|
| 活动名称 | 父亲节充值抵扣活动 |
| 核心玩法 | 用户充值到钱包余额，下单时余额自动抵扣订单金额的N% |
| 分销规则 | 仅充值产生佣金（二级分销），购物订单不再产生佣金 |
| 活动期限 | 可后台配置起止时间，到期自动关闭 |

---

## 二、用户操作流程

### 第一步：充值

```
用户打开小程序 → 点击"充值" → 选择充值金额 → 微信支付 → 钱包余额到账
```

- 充值金额不设最低限制
- 充值即送等额钱包余额（如充100元，钱包余额+100元）
- 充值成功后，上级分销员实时获得佣金（比例后台可配置）：
  - 一级上级：充值金额 × 2%（默认）
  - 二级上级：充值金额 × 1%（默认）

### 第二步：购物

```
用户选购商品 → 进入结算页 → 系统自动计算抵扣 → 微信支付剩余金额
```

#### 结算页展示示例

```
┌──────────────────────────────────┐
│  商品金额：         ¥500.00       │
│  运费：             包邮           │
│  ────────────────────────────    │
│  余额抵扣（40%）：  -¥200.00      │  ← 自动计算
│  ────────────────────────────    │
│  还需支付：         ¥300.00       │  ← 微信支付
│                                  │
│  钱包余额：¥800.00（抵扣后）       │
│                                  │
│  [ 微信支付 ¥300.00 ]            │
└──────────────────────────────────┘
```

### 第三步：完成

- 支付成功 → 订单生成 → 不产生购物佣金
- 钱包余额自动扣减已抵扣的200元

---

## 三、核心业务规则

### 3.1 抵扣规则

| 规则 | 说明 |
|------|------|
| 抵扣来源 | 用户钱包余额（user_money） |
| 抵扣比例 | 后台可配置，默认40% |
| 适用范围 | 订单商品总金额（不含运费，全部商品包邮） |
| 抵扣上限 | 不超过订单金额的N%，且不超过钱包余额 |
| 抵扣公式 | `实际抵扣 = MIN(订单金额 × 抵扣比例%, 钱包余额)` |
| 活动期外 | 抵扣开关关闭，全额微信支付，余额仍可用于全额支付 |

### 3.2 分销规则

| 规则 | 说明 |
|------|------|
| 购物佣金 | **关闭**（后台手动关闭分销开关即可） |
| 充值佣金 | 二级分销模式，一级20%、二级5%，实时到账 user_earnings |
| 佣金提现 | 不变，仍从 user_earnings 提现到微信/银行卡 |

### 3.3 余额规则

| 规则 | 说明 |
|------|------|
| 充值入口 | 不变，复用现有充值页面 |
| 余额用途 | 活动期间：抵扣购物 / 活动期外：全额支付购物 |
| 余额提现 | 不可提现 |
| 余额退款 | 订单退款时，抵扣部分退回钱包余额 |

---

## 四、后台配置项

### 新增：活动配置

| 配置键 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `activity_deduct_switch` | int | 0 | 充值抵扣开关：0=关闭 1=开启 |
| `activity_deduct_ratio` | int | 40 | 抵扣比例（百分比），如40表示可抵扣订单金额40% |
| `activity_start_time` | datetime | null | 活动开始时间 |
| `activity_end_time` | datetime | null | 活动结束时间 |

### 后台管理页面

在"应用 → 充值配置"或新建"活动管理"菜单下，增加以下配置表单：

```
┌─────────────────────────────────────┐
│  父亲节充值抵扣活动                   │
│                                     │
│  活动开关：    [ ○ 开启  ● 关闭 ]    │
│  抵扣比例：    [ 40 ] %              │
│  活动开始时间：[ 2026-06-01 00:00 ]  │
│  活动结束时间：[ 2026-06-30 23:59 ]  │
│                                     │
│  [ 保存 ]                           │
└─────────────────────────────────────┘
```

### 已有配置项（无需修改）

| 操作 | 说明 |
|------|------|
| 分销开关 | 在后台手动关闭购物分销，充值佣金不受影响 |
| 充值模板 | 复用现有充值面额配置 |
| 充值佣金比例 | 一级20%、二级5%，保持不变 |

---

## 五、技术改动点

### 5.1 后端改动

| 文件 | 改动内容 |
|------|---------|
| `OrderLogic` 下单结算 | 新增：计算抵扣金额 → 扣减 user_money → 生成微信支付金额 |
| `PayNotifyLogic::order()` | 支付回调中确认抵扣状态 |
| `RefundLogic` 退款 | 新增：退款时退回抵扣的余额 + 原路退回微信支付部分 |
| 活动配置 | 新增抵扣开关/比例/时间范围的配置项 |

### 5.2 前端改动

| 页面 | 改动 |
|------|------|
| 首页 | 增加父亲节活动入口（Banner/弹窗） |
| 结算页 | 显示抵扣明细行 + 剩余支付金额 + 微信支付按钮 |
| 充值页 | 添加活动主题装饰 |
| 商品详情页 | 可选活动角标 |

### 5.3 不动的部分

| 模块 | 状态 |
|------|------|
| 充值流程 | 完全复用 |
| 充值佣金 | 完全复用（二级分销，实时到账） |
| 分销等级 | 完全复用（按分销树深度） |
| 提现系统 | 完全复用（佣金提现） |
| 钱包余额 | 完全复用 user_money |
| 可提现余额 | 完全复用 user_earnings |

---

## 六、退款处理规则

| 场景 | 处理方式 |
|------|---------|
| 全额退款 | 微信支付部分原路退回 + 抵扣部分退回钱包余额 |
| 部分退款 | 按抵扣比例拆分：N%退钱包余额，(100-N)%退微信 |
| 退款时活动已结束 | 抵扣部分仍退回钱包余额（不改变退款的资金来源性质） |

**示例**：用户购买500元商品（余额抵扣200+微信支付300），退其中100元商品：
- 100 × 40% = 40元 → 退回钱包余额
- 100 × 60% = 60元 → 退回微信

---

## 七、前端主题设计要点

### 父亲节主题元素

- **主色调**：深蓝(#1a3a5c) + 暖金(#c9a96e) + 父亲节棕(#8b6914)
- **图标**：领带、胡须、父子、礼物等父亲节元素
- **首页**：活动Banner + "感恩父爱，充值有礼"标语
- **结算页**：抵扣行突出显示，强调"余额帮你省了¥XX"
- **充值页**：顶部增加父亲节主题背景装饰

---

## 八、待确认项

| # | 问题 | 建议 |
|---|------|------|
| 1 | 抵扣比例默认40%，最终确认？ | 40% |
| 2 | 活动时间范围？ | 如 2026-06-01 ~ 2026-06-30 |
| 3 | 活动结束后余额如何处理？ | 恢复为全额支付（现有逻辑不变） |
| 4 | 后台配置放在哪个菜单？ | 建议：应用 → 充值配置下新增活动配置 |
| 5 | 前端是否需要活动专属页面？ | 建议：首页加Banner，结算页加抵扣明细 |

---

> 以上方案待你审核确认后，进入开发阶段。